import Map from "../_components/Map";

export default async function Page() {
  return (
    <div className="w-full h-screen-custom flex flex-none relative overflow-hidden antialiased">
      <Map />
    </div>
  );
}
